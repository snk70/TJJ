@model IdentitySample.Models.RoleViewModel

@{
    ViewBag.Title = "ایجاد نقش جدید";
    Layout = "~/Views/Shared/_dashboard_layaout.cshtml";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>نقش ها</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.Name, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.TextBoxFor(model => model.Name, new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.Name)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="ایجاد نقش" class="btn btn-outline-primary" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("بازگشت به لیست نقش ها", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
